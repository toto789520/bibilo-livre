
module.exports = function addPage(data, props, event) {
    data.buttonlivre = props.livre
    data.buttonindex = props.index
    data.buttoncdpage = props.Page
    props.Pagefaite - props.Page
    data.buttoncdpagefaite = props.Pagefaite

    data.page = 'dicision'
    return data
}